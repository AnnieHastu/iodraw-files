```mermaid
graph TD
    subgraph 数据接入层 (感知)
        A[设备传感器/摄像头] --> B(多源数据采集);
        B --> C(视频数据抽帧/编码);
    end
    subgraph 数据中台与模型层 (大脑)
        D(流批一体计算) --> E{数据融合与分析};
        C --> D;
        B --> D;
        E --> F[智能运维模型];
        F --> G(模型持续优化);
        E --> H[多模态数据存储];
    end
    subgraph 业务应用层 (行动)
        I[前端可视化看板] --> J[自动化工单管理];
        F --> I;
    end
    H --> I;
    J --> K[运维人员/管理者];
```